{
  "name": "app-one",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "yarn asb --target release",
    "deploy": "near deploy --accountId=letseducate.msaudi.testnet --wasmFile=\"./build/release/app-one.wasm\"",
    "test": "node tests",
    "asbuild:untouched": "asc assembly/index.ts --target debug",
    "asbuild:optimized": "asc assembly/index.ts --target release",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@assemblyscript/loader": "^0.19.22",
    "assemblyscript-json": "^1.1.0",
    "jsonify": "^0.0.0",
    "near-sdk-as": "^3.2.3"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "assemblyscript": "^0.19.22"
  }
}
